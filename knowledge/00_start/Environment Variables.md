TO DO - Punch function into .bashrc that pulls env vars from file and displays them on every new terminal:
`export IP_ADDRESS=`
`export EMAIL=`
`export IP_RANGE=` e.g. 192.168.50.1-254`
`export DNS_SERVER=`
`export DOMAIN_NAME=`
`export=HOST_NAME=`