# Checklist
- [ ] ssh-audit.py
- [ ] Hydra (Brute Force SSH Login)
- [ ] Password Spraying (if there's a known password)
## ssh-audit 
`ssh-audit.py $IP_ADDRESS`
## Hydra (Brute Force SSH Login)
`hydra -l $USERNAME -P /usr/`